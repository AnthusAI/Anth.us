# ANTH-c28002 — consult this first

**Thrifty tokenmaxxing.** Newsroom story. Status: `research`. Copy draft exists (`article.md`). Do not publish until Ryan says so.

Short id: `ANTH-c28002`  
Full: `ANTH-c2800247-f8e3-4834-8bf1-a48eb127580e`  
This agent could not push `anthus-semantic-knowledge-base` (403). Canonical copies for other agents also live at `Anth.us/docs/newsroom-handoff/ANTH-c28002/` on **`develop`**.

If you have skb write access: copy these files onto `stories/ANTH-c2800247-f8e3-4834-8bf1-a48eb127580e/`, then `kbs comment` + `kbs validate`. Do not edit `project/issues/` by hand.

## What this story is

Public Anth.us how-to. Analog → Resist temptation → Monitor → Maximize included pools → **Context is the bill** (techniques) → per-app catalog. Mention spot tasks. Do not build Chattic/Kanbus from this card.

## Ryan’s locked calls (2026-09-01)

- Working title: Thrifty tokenmaxxing (he may rename).
- Lead: free first hit that isn’t free. Adversarial. Don’t open on a spreadsheet.
- Principles: Resist temptation. Monitor. Maximize **included** pools only (not Fast, not on-demand).
- **Context is the bill** is a **techniques catalog**, not a slogan. Record **all** of them in the draft; proofreading may cut later.
- Technique 1 architecture: **manager = long thread (compact separately); each worker = fresh context with only the brief.** `/clear` is the mission reset. Standing MCP/rules on children are a settings caveat.
- Sub-agent thrift: isolation can save because workers don’t re-bill the manager junk drawer. Parallelism multiplies windows. Then **Jevons**: cheaper tasks → more work → total can rise. One beat, link `ecc2ae`. Don’t retell.
- Spin-off name locked: **spot tasks** (like spot instances). Parked. Chattic feature page + Kanbus queue later. Not `bef418` leftover *gates*.
- Firsthand: Cursor **keeps flipping Fast back on** during async work. One beat, link Never Use Fast. Don’t reprint the tax table. Verify sticky-default. He said “doubles”; check live multiples (Composer Fast was 6×).
- Monitor community: **CodexBar** (live leftover) and **ccusage** (historical logs). Not tiny `aiuse`/`aiquota`.
- Don’t publish until Ryan says so.

## File index (read in this order)

| File | What it is |
| --- | --- |
| `README.md` | This index. Start here. |
| `article.md` | **Copy draft.** Gemini 3.7 Flash High + editor pass. Newsroom markdown, not site MDX. |
| `article.gemini-raw.md` | Unedited Gemini output. |
| `idea.md` | Pitch, spine, split, parked spot tasks. |
| `assignment.md` | What to write. Must / must not. |
| `research.md` | Running editorial log + verify table. |
| `context-techniques.md` | All input-volume techniques to put in the draft. |
| `subagent-context.md` | Per-product: does the child inherit parent chat? |
| `subagent-savings.md` | Simple money-saving worker moves + Jevons catch. |
| `principles-alignment.md` | How we line up with outside writing. |
| `prior-art.md` | External settings guides. |
| `quota-community.md` | Maintained quota-monitor projects (CodexBar, ccusage, …). |
| `quota-dispatch.md` | Spot tasks research + parked multi-repo spec. |
| `cursor-auto-factcheck.md` | Auto vs pin Composer vs Router receipts. |

## Do not

- Don’t reprint Never Use Fast’s tax table (`8bde91`).
- Don’t re-coin Maximize Value or “tokenmaxxing for fun and profit.”
- Don’t retell Jevons from the commodity article.
- Don’t merge with `bef418`, `ef4df5`, `629b47`.
- Don’t invent quota numbers, model ids, or Codex `fork_turns` default.
- Don’t implement Chattic, Kanbus, or a spot-tasks product from this story.
