---
title: "Classification with Confidence"
slug: "classification-with-confidence"
date: "2025-09-15"
display_date: "September 2025"
authors:
  - author: <a href="/ryan">Ryan Porter</a>
tags: ["solutions", "featured"]
preview_image: "../images/llm-classifiers/freud-detector-fine-tuned.png"
excerpt: "<p>A practical example of aligning a classifier to business requirements with fine-tuning, evaluation, and confidence thresholds for human review. The point is not just better accuracy. It is building a classification workflow that knows when to automate, when to escalate, and how to improve over time. View the project on <a href='https://github.com/AnthusAI/Classification-with-Confidence'>GitHub</a>.</p>"
images:
  - "../images/llm-classifiers/freud-detector-base.png"
  - "../images/llm-classifiers/freud-detector-fine-tuned.png"
state: published
---

import BlogImage from '../../components/blog-image';

## Fine-Tuning LLMs for Better Classification

When building text classification systems, a key question emerges: how much can LLM fine-tuning improve classification accuracy compared to using base models with prompt engineering alone?

This project demonstrates the practical application of **LLM fine-tuning** to align a classifier with specific business requirements. More importantly, it shows how to turn classification into a governed workflow: measure performance, estimate confidence, and use that confidence to decide when human review is required.

## The Problem: When to Trust AI Decisions

In production AI systems, especially those involving **human-in-the-loop (HITL)** workflows, a critical question arises: when should the system make autonomous decisions, and when should it escalate to human review?

**Confidence scoring** provides the answer. By understanding not just *what* the model predicts but *how confident* it is in that prediction, we can:

- Route high-confidence predictions straight through
- Flag low-confidence cases for human review
- Optimize the balance between automation and accuracy
- Continuously improve by learning from the cases humans review

## The Approach: Fine-Tuning for Alignment

We took a straightforward classification task and compared two approaches:

1. **Base model with prompt engineering**: Using GPT-4o-mini out of the box
2. **Fine-tuned model**: Training the same model on labeled examples

The results speak for themselves:

### Before Fine-Tuning

<BlogImage 
  images={props.pageContext.frontmatter.images} 
  name="freud-detector-base.png"
  alt="Confusion matrix showing base model performance" 
/>

The base model shows decent performance but struggles with false negatives—missing positive cases that should be caught.

### After Fine-Tuning

<BlogImage 
  images={props.pageContext.frontmatter.images} 
  name="freud-detector-fine-tuned.png"
  alt="Confusion matrix showing fine-tuned model performance" 
/>

The fine-tuned model demonstrates significantly improved accuracy, particularly in correctly identifying positive cases (8 correct vs 4 in the base model).

## Why This Matters

For us, the value of **LLM fine-tuning** is not résumé signaling or model novelty. It is that better alignment creates better operating conditions for the surrounding system:

- High-confidence cases can flow straight through
- Low-confidence cases can be routed to human review
- Teams get a cleaner tradeoff between speed and accuracy
- Feedback from reviewed cases can be reused to improve the model

Those techniques enable:

- Customization for domain-specific requirements
- Better handling of edge cases and terminology
- Reduced false positives and false negatives
- Confidence scoring integrated into **adaptive systems**

### Fine-Tuning as Part of RLHF Systems

Fine-tuning a classifier can be a key component of larger **RLHF (Reinforcement Learning from Human Feedback)** systems. When humans review AI decisions and provide feedback, that feedback can be reused for better training and evaluation, creating a continuous learning loop:

1. **AI makes predictions** with confidence scores
2. **Humans review** low-confidence or flagged cases
3. **Feedback is captured** as training data
4. **Model is fine-tuned** to align better with human decisions
5. **System improves** over time, requiring less human review

This is how you build systems that get better through human feedback. Fine-tuning is not just a one-time optimization. It is one mechanism for keeping the model aligned to human judgment as the workflow evolves.

## Technical Implementation

The project includes:

- **Training pipeline**: Scripts for fine-tuning OpenAI models
- **Evaluation framework**: Tools for measuring classification performance
- **Confidence analysis**: Methods for extracting and using confidence scores
- **Comparison tools**: Side-by-side evaluation of base vs fine-tuned models

This represents the kind of practical ML engineering that bridges research and production deployment.

## Production-Ready AI Thinking

What separates demos from production systems is attention to details like:

- **Model alignment**: Fine-tuning to match specific requirements
- **Confidence scoring**: Knowing when to trust automated decisions
- **Continuous evaluation**: Measuring performance on real-world data
- **Human-in-the-loop integration**: Designing for human oversight where needed

These are the controls that let a classifier participate in a real workflow instead of sitting in a notebook demo.

## Open Source

The complete project is available on [GitHub](https://github.com/AnthusAI/Classification-with-Confidence), including training scripts, evaluation tools, and example datasets. It's designed to be a practical reference for anyone implementing similar classification systems.

This work emerged from real-world challenges in building production AI systems, and we are sharing it as a concrete reference for teams trying to build classification workflows that remain testable and governable.

